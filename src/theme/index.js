import Colors from './defaults/colors';
import Base from './defaults/base';
import Opacity from './defaults/opacity';
import Palette from './defaults/palette';
import Variables from './variables';

const Theme = {
  Colors,
  Base,
  Opacity,
  Palette,
  Variables,
};

export default Theme;
