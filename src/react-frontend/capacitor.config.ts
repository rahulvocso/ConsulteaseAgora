import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.consultease.com",
  appName: "CallingApp",
  webDir: "build",
  bundledWebRuntime: false,
  cordova: {},
};

export default config;
